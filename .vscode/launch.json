{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch BevyBird",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/bevy_bird",
            "args": [],
            "cwd": "${workspaceRoot}",
        }
    ]
}